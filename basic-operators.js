// assignmet operator (=)
let a = 12;

//The Mathematical operators
//the addition or concatiation operator (+)
let b = 23 + 239;
let c = "hello" + " " + "world!";

// the subtraction operator (-)
let d = 273 - 2;

// The multiplication operator (*)
let e = 23 * 2;

// The division operator (/)
let f = 23 / 3;

// the modulus (remainder) operator (%)
let g = 50 % 3;

// the power operator (**)
let h = 2 ** 5;


// the incrementing operator(++)
let i = 0;
i++;

// the decrementing operator(--)
let j = 5;
j--;


// the comparism operators 
// The greater than operator (>)
let k = 3 > 4;

// the less than operator (<)
let l = 3 < 4

// the equaliity operator (==)
let m = 4 == "4";

// the strict equality operator (===)
let n = 4 === "4";

// the not equalt to operator (!=)
let o = 6 != 5;

// the greater or equal to operator (>=)
let p = 3 >= 3;

// the less than or equal to operator (<=)
let q = 3 <= 6;

//The logical operators
// The OR operator (||)
let r = true || true; //true
let r1 = true || false;  //true
let r2 = false || true; //true
let r3 = false || false; //false

// the AND opertor (&&)
let s = true && true; //true
let s1 = true && false; //false
let s2 = false && true; //false
let s3 = false && false; //false

